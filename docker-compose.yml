version: "3.8"
services:
  pyshell:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8080
      - DATABASE_URL=sqlite:///data/pyshell.db
      - SECRET_KEY=change-me-in-production